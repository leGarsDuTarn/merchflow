<div class="bg-light min-vh-100 py-5">
  <div class="container">

    <div class="row justify-content-center mb-4">
      <div class="col-lg-10">
        <div class="d-flex align-items-center justify-content-between">
          <div class="d-flex align-items-center">
            <%# Retour vers le show de l'offre ou l'index %>
            <%= link_to fve_job_offer_path(@job_offer), class: "btn btn-white bg-white rounded-circle shadow-sm me-4 p-0 d-flex align-items-center justify-content-center transition-hover text-decoration-none", style: "width: 55px; height: 55px; border: 1px solid #eee;" do %>
              <i class="fa-solid fa-arrow-left text-muted fs-5"></i>
            <% end %>

            <div>
              <div class="d-flex align-items-center gap-2">
                <span class="badge bg-orange-light text-orange border border-orange-subtle rounded-pill px-2 py-1 small fw-bold">
                  EDITION
                </span>
                <span class="text-muted small fw-bold text-uppercase ls-1">Espace Recruteur</span>
              </div>
              <h2 class="fw-bold text-dark m-0 mt-1" style="letter-spacing: -0.5px;">
                Modifier l'offre #<%= @job_offer.id %>
              </h2>
            </div>
          </div>

          <div class="text-end">
            <%= link_to fve_job_offer_path(@job_offer),
                        data: { turbo_method: :delete, turbo_confirm: "⚠️ Attention : Cette action est irréversible. Souhaitez-vous vraiment supprimer cette annonce ?" },
                        class: "btn btn-outline-danger btn-sm rounded-pill fw-bold px-3 transition-hover" do %>
              <i class="fa-solid fa-trash-can me-1"></i> Supprimer l'offre
            <% end %>
          </div>
        </div>
      </div>
    </div>

    <div class="row justify-content-center">
      <div class="col-lg-10">
        <%= render 'form', job_offer: @job_offer %>
      </div>
    </div>

    <div class="row justify-content-center mt-4">
      <div class="col-lg-10 text-center">
        <p class="text-muted small">
          <i class="fa-solid fa-circle-info me-1"></i>
          Les modifications seront visibles immédiatement par les candidats après enregistrement.
        </p>
      </div>
    </div>

  </div>
</div>

<style>
  /* On garde la cohérence stylistique MerchFlow */
  .form-control:focus, .form-select:focus, .form-check-input:focus {
    border-color: #fd7e14 !important;
    box-shadow: 0 0 0 0.25rem rgba(253, 126, 20, 0.15) !important;
    background-color: #fff !important;
  }

  .btn-white { background-color: #fff; }

  .transition-hover {
    transition: all 0.2s ease-in-out;
  }
  .transition-hover:hover {
    transform: translateY(-2px);
    box-shadow: 0 0.5rem 1rem rgba(0,0,0,0.08) !important;
  }

  .bg-orange-light { background-color: #fff3e0; }
  .text-orange { color: #fd7e14; }
  .ls-1 { letter-spacing: 0.5px; }

  /* Masquer les flèches sur les inputs de taux */
  input[type=number]::-webkit-inner-spin-button,
  input[type=number]::-webkit-outer-spin-button {
    -webkit-appearance: none;
    margin: 0;
  }
</style>
