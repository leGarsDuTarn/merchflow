<div class="container mt-4 mb-5">

  <!-- TITRE -->
  <h1 class="fw-bold text-orange text-center mb-4">
    Recherche de Merchandisers
  </h1>

  <!-- FORMULAIRE DE RECHERCHE AVANCÉE -->
  <div class="card shadow-sm rounded-4 p-4 mb-5">
    <%= form_with url: fve_merch_index_path, method: :get, local: true, class: "row g-3" do |f| %>

      <!-- LOCALISATION -->
      <div class="col-md-6 col-lg-4">
        <%= f.label :city, "Ville", class: "form-label fw-semibold" %>
        <%= f.text_field :city,
            value: params[:city],
            class: "form-control",
            placeholder: "Ex: Paris, Lyon..." %>
      </div>

      <div class="col-md-6 col-lg-4">
        <%= f.label :zipcode, "Code postal", class: "form-label fw-semibold" %>
        <%= f.text_field :zipcode,
            value: params[:zipcode],
            class: "form-control",
            placeholder: "Ex: 75001" %>
      </div>

      <div class="col-md-6 col-lg-4">
        <%= f.label :department, "Département", class: "form-label fw-semibold" %>
        <%= f.text_field :department,
            value: params[:department],
            class: "form-control",
            placeholder: "Ex: 75, 69..." %>
      </div>

      <!-- DISPONIBILITÉ -->
      <div class="col-md-6 col-lg-4">
        <%= f.label :start_date, "Disponible du", class: "form-label fw-semibold" %>
        <%= f.date_field :start_date,
            value: params[:start_date],
            class: "form-control" %>
      </div>

      <div class="col-md-6 col-lg-4">
        <%= f.label :end_date, "Au", class: "form-label fw-semibold" %>
        <%= f.date_field :end_date,
            value: params[:end_date],
            class: "form-control" %>
      </div>

      <!-- EXPÉRIENCE -->
      <div class="col-md-6 col-lg-4">
        <%= f.label :company, "A travaillé pour", class: "form-label fw-semibold" %>
        <%= f.text_field :company,
            value: params[:company],
            class: "form-control",
            placeholder: "Nom de l'entreprise" %>
      </div>

      <!-- FILTRES SUPPLÉMENTAIRES -->
      <div class="col-md-6 col-lg-4">
        <div class="form-check mt-4">
          <%= f.check_box :has_contract_with_me,
              { class: "form-check-input", checked: params[:has_contract_with_me] == "1" },
              "1", "0" %>
          <%= f.label :has_contract_with_me, "A déjà un contrat avec ma FVE", class: "form-check-label" %>
        </div>
      </div>

      <div class="col-md-6 col-lg-4">
        <div class="form-check mt-4">
          <%= f.check_box :only_with_contact,
              { class: "form-check-input", checked: params[:only_with_contact] == "1" },
              "1", "0" %>
          <%= f.label :only_with_contact, "Coordonnées visibles uniquement", class: "form-check-label" %>
        </div>
      </div>

      <!-- BOUTONS -->
      <div class="col-12 d-flex gap-2 justify-content-end mt-4">
        <%= f.submit "Rechercher", class: "btn btn-orange fw-semibold px-4" %>
        <%= link_to "Réinitialiser", fve_merch_index_path, class: "btn btn-green" %>
      </div>

    <% end %>
  </div>

  <!-- RÉSULTATS -->
  <% search_params = %w[city zipcode department start_date end_date company has_contract_with_me only_with_contact] %>
  <% has_search = search_params.any? { |key| params[key].present? } %>

  <% if has_search %>
    <p class="text-muted mb-3">
      <strong><%= @merch.count %></strong> résultat(s) trouvé(s)
    </p>
  <% end %>

  <% if @merch.any? %>
    <div class="row g-4">

      <% @merch.each do |m| %>
        <div class="col-md-6 col-lg-4">
          <div class="card shadow-sm rounded-4 p-3 h-100">

            <!-- NOM -->
            <h5 class="fw-bold text-orange mb-1">
              <%= m.displayable_name(current_user) %>
            </h5>

            <!-- LOCALISATION -->
            <p class="text-muted mb-2">
              <strong>Ville :</strong>
              <%= m.city.presence || "—" %>
              <br>
              <strong>Code postal :</strong>
              <%= m.zipcode.presence || "—" %>
            </p>

            <!-- CONTRAT EXISTANT -->
            <% if m.contracts.where(agency: current_user.agency).exists? %>
              <p class="mb-2">
                <span class="badge bg-success">✓ Contrat existant avec votre FVE</span>
              </p>
            <% end %>

            <!-- CONTACT -->
            <div class="mb-2 small">
              <strong>✉️ Email :</strong>
              <%= m.displayable_email(current_user) || "<span class='text-muted'>Non visible</span>".html_safe %>
              <br>
              <strong>📞 Téléphone :</strong>
              <%= m.displayable_phone(current_user) || "<span class='text-muted'>Non visible</span>".html_safe %>
            </div>

            <!-- DISPONIBILITÉ (si dates recherchées) -->
            <% if params[:start_date].present? || params[:end_date].present? %>
              <% unavailable_dates = m.unavailabilities.pluck(:date) %>
              <% if unavailable_dates.empty? %>
                <p class="text-success small mb-2">✓ Disponible sur la période</p>
              <% end %>
            <% end %>

            <!-- BOUTON -->
            <div class="mt-auto d-grid">
              <%= link_to "Voir le profil détaillé",
                    fve_merch_path(m),
                    class: "btn btn-orange fw-semibold btn-sm" %>
            </div>

          </div>
        </div>
      <% end %>

    </div>

  <% else %>
    <div class="text-center mt-5">
      <p class="text-muted">
        <%= has_search ?
            "🔍 Aucun merchandiser ne correspond à vos critères." :
            "📋 Utilisez les filtres ci-dessus pour rechercher des merchandisers." %>
      </p>
    </div>
  <% end %>

</div>
